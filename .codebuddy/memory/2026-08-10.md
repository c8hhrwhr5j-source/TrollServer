# 2026-08-10

## 修复重启功能
- 仓库从 `c8hhrwhr5j-source/TrollServer` 克隆，切换到 `master` 分支获取最新代码
- 发现"重启设备"按钮实际行为是关机（reboot() 在 TrollStore 下被 AMFI 拦截）
- 方案演进：方案 1-7 均失败，最终采用方案 8：`kill(1, SIGKILL)` 杀死 launchd 触发内核 panic 强制重启
- 文件改动：`reboot_helper.c` 将 `reboot(0)` 替换为 `kill(1, SIGKILL)`
- UI 暂时保留"关闭设备"文字，待测试验证后改回
- 已推送到 GitHub: `2374e61` 和 `7daf73a`
